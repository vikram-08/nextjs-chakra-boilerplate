/* The code is defining a JavaScript object called `Button` with a property `baseStyle`. The
`baseStyle` property has a value of another object with a property `borderRadius` set to the value
`"full"`. */

const Button = {
  baseStyle: {
    borderRadius: "full",
  },
};

export default Button;
