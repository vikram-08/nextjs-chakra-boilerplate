/* The code is defining a constant variable named `Input` which is an object. The object has a property
`baseStyle` which is also an object. Inside the `baseStyle` object, there is a property `field`
which is an object as well. The `field` object has a property `borderRadius` with a value of
`"full"`. */

const Input = {
  baseStyle: {
    field: {
      borderRadius: "full",
    },
  },
};

export default Input;
