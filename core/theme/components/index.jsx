import Button from "@/theme/components/button";
import Input from "@/theme/components/input";
import Divider from "@/theme/components/divider";

/* The code `export const components = { Button, Input };` is exporting an object named `components`
that contains two properties: `Button` and `Input`. These properties are assigned the values of the
`Button` and `Input` components respectively. */

const components = {
  Button,
  Input,
  Divider,
};

export default components;
